CREATE SEQUENCE SEQ_PRODUTO START 1 INCREMENT 1;
CREATE TABLE PRODUCT (ID INT4 NOT NULL, DESCRIPTION VARCHAR(255) NOT NULL, NAME VARCHAR(100) NOT NULL, PRICE NUMERIC(19, 2) NOT NULL CHECK (PRICE>=0), PRIMARY KEY (ID));